name: 🛡️ Production DevSecOps Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # <--- Isso cria o botão manual!

jobs:
  build-and-test:
    name: 🏗️ Build & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

  security-gate:
    name: 🔒 Security Gates (Shift-Left)
    needs: [build-and-test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3

      - name: 🔍 Run CodeQL SAST Analysis
        run: |
          echo "Starting semantic analysis..."
          sleep 2
          echo "::error::Critical Security Vulnerability Detected!"
          exit 1
